package IfStatements;

import javax.swing.*;

public class imBeingStupid{

public static void main (String [] args){

   String userAns;
   userAns = JOptionPane.showInputDialog ("What is two times two bozo");
   
   int Option = Integer.parseInt (userAns);
   
   
   if (Option == 4){
      System.out.println ("I was mistaken, you are not a bozo... youre right. #gaslighting gatekeeping and girlbossing. You are slaying (im not gay sorry for the cultural appropiating)");
   }
   
    else 
 {
      System.out.println ("Wrong. L + ratio + bozo + kys + cry about it emo.");
      
      //Set up variables
      //get input
      //set new variable to store option in (int/ string = Option)
      //if input needs to be a number, convert using Integer.parseInt ()
      //if (condition == num) --> use double equals 
      //new code block with code inside
      //new code block, with else checker and what to do inside 
      // eg. if{   new line }
      //eg. else {   new line }
 }

   }
   
   
   
   
   
   
   
   
   
   
   
}